{
  "_comment": "Immutable scoring config for the core-api.yaml loop. The agent must NEVER edit this. score(spec) = (control_violations + arch_violations) * big_penalty + complexity. Keep iff score(candidate) < score(best): feasibility first (drive violations down), then minimize complexity.",

  "weights": {
    "_comment": "Complexity weights. concept is heaviest so deleting/merging concepts pays most.",
    "concept": 10,
    "field": 1,
    "endpoint": 3,
    "task": 2,
    "generic": 5,
    "money_float": 8,
    "enum": 1,
    "global_dl": 0.0001,
    "conformance_event": 2,
    "conformance_timer": 2,
    "namespace_gap": 1,
    "redundancy": 0.25
  },
  "enum_cap": 12,
  "_enum_note": "weights.enum is charged per enum value above enum_cap, summed across every enum in the doc. Closes a blind spot: enum VALUES are schema-internal constraints the parser never lifts into the vocabulary, so without this term a 337-value Task.type cost zero and the minimizer could neither see nor delete it. Cap 12 leaves normal status/type enums free; a 337-value enum contributes 325, making its collapse to the ~28 generic verbs a large, keep-worthy score win. The companion 'unused supply' signal (control_oracle: fields/events no control cites) is reported in score.coverage as deletion candidates for the proposer, but is intentionally NOT a score gate — much uncited supply is architecture-required or contract plumbing, and the arch gate already protects load-bearing elements.",
  "_money_float_note": "Penalty per money amount typed as float (`number`). Money must be integer minor units in a ledger system — float rounding drift breaks Sum(balances)==settlement invariants invisibly. Ratios/scores/rates legitimately stay number (see fitness.MONEY_WORDS / NONMONEY_WORDS). Weighted above `field` so converting amounts to integer is a score-improving move the loop keeps.",

  "global_dl": false,
  "_global_dl_note": "Off by default (API-only fitness, cheap). Set true once the outer loop lands to also penalize pushing complexity from the spec into controls.json / control-vocabulary.json (byte-length term).",

  "_redundancy_note": "Weight on factoring_oracle's compressible surplus (sum over property names appearing in >= cluster_min objects of occurrences-1). Repeated field clusters are description length the element count cannot see: extract the cluster into a base schema members compose via allOf:[$ref], and the copies collapse to one definition + N references. Weighted 0.25 (gentle) because the real payoff is realized through the field/concept accounting once the extract_base move lands — this term only tilts ties toward factoring and keeps the surplus VISIBLE in the score and prompt. Anti-gaming: surplus drops only when properties actually move out of members into a referenced base; an unreferenced base changes nothing, and the concept weight (10x) on the new base prevents over-shattering into micro-mixins. Behind structural_terms; semantic-distinctness risk (a shared name that is genuinely two concepts) is handled by keep-iff-lower + human review of the extract move, not forced merges.",

  "invariant_gate_note": "invariant_oracle violations (x-actions action not a registered event type, transitions_to not a declared state, x-timers task_type not registered, dangling $ref) fold into the big_penalty gate alongside control/arch coverage. Currently 0 by construction; the gate keeps it 0 under optimization pressure — it does not need a weight (a violation costs big_penalty like any other gap).",

  "structural_terms": true,
  "_structural_terms_note": "Turns on the conformance_oracle irregularity surcharge: score += conformance_event*noncanonical_events + conformance_timer*noncanonical_timers + namespace_gap*namespace_gaps. This deepens the objective from 'minimize element count' to 'minimize description length subject to regularity' — a spec whose codes all decompose into registered object.property.action / object.task_type.due primitives is shorter to describe generatively than N bespoke codes of equal count, and that compression is exactly what the count-based complexity term is blind to. It makes the meta-structure moves (decompose code to canonical form, register an event-implied field, fold a per-domain *_due_at into the Task pattern) score-LOWERING, so the proposer proposes and KEEPS them instead of having them auto-reverted. A soft surcharge, not a gate (13% non-conformance must not make the spec infeasible). Anti-gaming: every term requires a real registered primitive (action in x-event-types, task_type in x-task-types, property as a schema field) and the demanded codes come from controls.json (immutable), so dot-shape renaming earns nothing. conformance_event/timer are weighted 2 (above field=1, below concept=10) so registering a reused action to make many codes canonical pays, but minting a junk action does not. Set false to revert to pure count-minimization.",

  "big_penalty": 100000,

  "control_budget": 0,
  "_control_budget_note": "Allowed unregistered codes before it counts as a violation. control_violations = max(0, unregistered - control_budget). Default 0 = hard coverage (every cited code must be registered). The user's chosen default is a PROVISIONAL budget: set this to the current measured baseline (live full-repo = 1116; a fair-lending-scoped phase-1 run will be far smaller) so the loop starts in the minimize-complexity regime instead of having to register every code first. Tighten toward 0 as coverage improves. The keep-iff-best rule has teeth at any budget because it is relative: any move that raises unregistered raises the score.",

  "arch_budget": 0,
  "_arch_budget_note": "Allowed uncovered architectural elements before it counts as a violation. arch_violations = max(0, uncovered - arch_budget). Default 0 = hard architecture coverage. Current measured baseline uncovered = 56 with endpoint_derivation='stateful' (the missing banking-core endpoints + per-resource REST the derivation now requires); ~186 with 'all'. Endpoints are now DERIVED from resources (see architecture-spec.json endpoint_derivation), so resource-backed endpoints like /cases are load-bearing and cannot be deleted for free. Set a provisional arch_budget if you want to minimize complexity before building out endpoints."
}
