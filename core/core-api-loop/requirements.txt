# Dependencies for the core-api.yaml self-minimizing loop (core-api-loop/).
# Set up:  python3.12 -m venv core-api-loop/.venv && core-api-loop/.venv/bin/pip install -r core-api-loop/requirements.txt
PyYAML>=6.0
# Only needed for the LLM backends (propose.py / regenerate.py --backend api). Reads the key
# from the ANTHROPIC_API_KEY environment variable — never hard-code or commit a key.
anthropic>=0.40
