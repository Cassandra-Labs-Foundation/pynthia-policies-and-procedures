# The proposed surface — designed, not yet implemented

> GENERATED by `scripts/gen_proposed_surface.py` (runs in
> `scripts/rebuild_artifacts.sh`). Do not edit by hand.
>
> These operations live in `core/core-api.yaml`'s `x-proposed-paths`
> block: designed — mostly by `core/core-api-loop/` from accumulated
> policy demand — but with **no route and no handler**, and excluded
> from route parity on purpose. An operation graduates by moving to
> `paths:` with an `x-handler` and an implementation (the workflow is
> in `CLAUDE.md`). Lending-adjacent entries stay parked by the
> narrow-bank decision recorded in `api/index.ts`.

**118 operations across 32 resources.**

| resource | ops | schema exists | verifier waits on it |
|---|---|---|---|
| `/account-numbers` | 3 | yes |  |
| `/ach-transfers` | 2 | yes |  |
| `/auth` | 1 |  |  |
| `/cases` | 4 | yes | yes |
| `/changes` | 4 | yes | yes |
| `/cois` | 4 | yes | yes |
| `/complaints` | 3 | yes |  |
| `/control-results` | 1 | yes |  |
| `/disputes` | 3 | yes |  |
| `/documents` | 3 | yes |  |
| `/entities` | 4 |  |  |
| `/events` | 1 | yes |  |
| `/fbo` | 2 |  |  |
| `/filings` | 6 | yes | yes |
| `/findings` | 4 | yes | yes |
| `/handovers` | 4 | yes | yes |
| `/inbound` | 1 |  |  |
| `/incidents` | 4 | yes |  |
| `/indemnifications` | 4 | yes | yes |
| `/insiders` | 4 | yes | yes |
| `/legal-holds` | 3 | yes |  |
| `/loan-applications` | 7 | yes | yes |
| `/loans` | 5 | yes | yes |
| `/payments` | 4 |  |  |
| `/records-packages` | 4 | yes | yes |
| `/risks` | 4 | yes | yes |
| `/sandbox` | 10 |  |  |
| `/tasks` | 5 | yes | yes |
| `/trades` | 4 | yes | yes |
| `/trainings` | 4 | yes | yes |
| `/verifications` | 4 | yes | yes |
| `/wire-transfers` | 2 | yes |  |

## Operations by resource

### /account-numbers

- `GET /account-numbers` — List account-numbers
- `POST /account-numbers` — Create AccountNumber
- `GET /account-numbers/{id}` — Get AccountNumber

### /ach-transfers

- `POST /ach-transfers` — Create AchTransfer
- `POST /ach-transfers/{id}/transition` — Transition AchTransfer

### /auth

- `POST /auth/token` — Issue a short-lived instance token

### /cases

- `GET /cases` — List cases
- `POST /cases` — Open case
- `GET /cases/{id}` — Get case
- `POST /cases/{id}/transition` — Transition case

### /changes

- `GET /changes` — List change
- `POST /changes` — Create change
- `GET /changes/{id}` — Get change
- `POST /changes/{id}/transition` — Transition change

### /cois

- `GET /cois` — List coi
- `POST /cois` — Create coi
- `GET /cois/{id}` — Get coi
- `POST /cois/{id}/transition` — Transition coi

### /complaints

- `GET /complaints` — List complaint
- `GET /complaints/{id}` — Get complaint
- `POST /complaints/{id}/transition` — Transition complaint

### /control-results

- `GET /control-results/{id}` — Get control result

### /disputes

- `GET /disputes` — List dispute
- `GET /disputes/{id}` — Get dispute
- `POST /disputes/{id}/transition` — Transition dispute

### /documents

- `GET /documents` — List documents
- `POST /documents` — Create document
- `GET /documents/{id}` — Get document

### /entities

- `POST /entities/business` — Create a business entity
- `POST /entities/joint` — Create a joint entity
- `POST /entities/person` — Create a person entity
- `POST /entities/trust` — Create a trust entity

### /events

- `GET /events` — List events

### /fbo

- `GET /fbo/{instance_id}/available_balance` — FBO available balance
- `GET /fbo/{instance_id}/position` — FBO position for an instance

### /filings

- `GET /filings` — List filings
- `POST /filings` — Prepare filing
- `GET /filings/{id}` — Get filing
- `POST /filings/{id}/acknowledge` — Acknowledge filing
- `POST /filings/{id}/submit` — Submit filing
- `POST /filings/{id}/transition` — Transition Filing

### /findings

- `GET /findings` — List finding
- `POST /findings` — Create finding
- `GET /findings/{id}` — Get finding
- `POST /findings/{id}/transition` — Transition finding

### /handovers

- `GET /handovers` — List handover
- `POST /handovers` — Create handover
- `GET /handovers/{id}` — Get handover
- `POST /handovers/{id}/transition` — Transition handover

### /inbound

- `GET /inbound/{instance_id}` — Inbound payments routed to an instance

### /incidents

- `GET /incidents` — List
- `GET /incidents/{id}` — Get
- `PATCH /incidents/{id}` — Update
- `POST /incidents/{id}/transition` — Transition

### /indemnifications

- `GET /indemnifications` — List indemnification
- `POST /indemnifications` — Create indemnification
- `GET /indemnifications/{id}` — Get indemnification
- `POST /indemnifications/{id}/transition` — Transition indemnification

### /insiders

- `GET /insiders` — List insider
- `POST /insiders` — Create insider
- `GET /insiders/{id}` — Get insider
- `POST /insiders/{id}/transition` — Transition insider

### /legal-holds

- `GET /legal-holds` — List legal holds
- `POST /legal-holds` — Place legal hold
- `POST /legal-holds/{id}/release` — Release legal hold

### /loan-applications

- `GET /loan-applications` — List
- `POST /loan-applications` — Create
- `GET /loan-applications/{id}` — Get
- `PATCH /loan-applications/{id}` — Update
- `POST /loan-applications/{id}/counteroffer` — Issue counteroffer
- `POST /loan-applications/{id}/decision` — Record decision
- `POST /loan-applications/{id}/transition` — Transition LoanApplication

### /loans

- `GET /loans` — List
- `POST /loans` — Create Loan
- `GET /loans/{id}` — Get
- `POST /loans/{id}/book` — Request booking
- `POST /loans/{id}/transition` — Transition Loan

### /payments

- `POST /payments/ach/originate` — Originate an ACH payment
- `POST /payments/rtp/originate` — Originate an RTP payment
- `POST /payments/wire/confirm` — Confirm a reserved wire
- `POST /payments/wire/originate` — Originate a wire payment

### /records-packages

- `GET /records-packages` — List records_package
- `POST /records-packages` — Create records_package
- `GET /records-packages/{id}` — Get records_package
- `POST /records-packages/{id}/transition` — Transition records_package

### /risks

- `GET /risks` — List risk
- `POST /risks` — Create risk
- `GET /risks/{id}` — Get risk
- `POST /risks/{id}/transition` — Transition risk

### /sandbox

- `POST /sandbox/simulate/ach/receive` — Simulate an inbound ACH
- `POST /sandbox/simulate/ach/return` — Simulate an ACH return
- `POST /sandbox/simulate/ach/settle` — Simulate ACH settlement
- `POST /sandbox/simulate/card/authorize` — Simulate a card authorization
- `POST /sandbox/simulate/card/settle` — Simulate card settlement
- `POST /sandbox/simulate/kyc/approve` — Simulate KYC approval
- `POST /sandbox/simulate/kyc/deny` — Simulate KYC denial
- `POST /sandbox/simulate/time/advance` — Advance sandbox clock
- `POST /sandbox/simulate/wire/receive` — Simulate an inbound wire
- `POST /sandbox/simulate/wire/settle` — Simulate wire settlement

### /tasks

- `GET /tasks` — List tasks
- `POST /tasks` — Create Task
- `GET /tasks/{id}` — Get task
- `POST /tasks/{id}/complete` — Complete task
- `POST /tasks/{id}/transition` — Transition Task

### /trades

- `GET /trades` — List trade
- `POST /trades` — Create trade
- `GET /trades/{id}` — Get trade
- `POST /trades/{id}/transition` — Transition trade

### /trainings

- `GET /trainings` — List training
- `POST /trainings` — Create training
- `GET /trainings/{id}` — Get training
- `POST /trainings/{id}/transition` — Transition training

### /verifications

- `GET /verifications` — List verifications
- `POST /verifications` — Create Verification
- `GET /verifications/{id}` — Get Verification
- `POST /verifications/{id}/transition` — Transition Verification

### /wire-transfers

- `POST /wire-transfers` — Create WireTransfer
- `POST /wire-transfers/{id}/transition` — Transition WireTransfer

